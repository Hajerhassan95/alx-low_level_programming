#include "main.h"

/**
 * print_sign - checks the n if greater, less or equal 0
 *
 * @n: takes input from other functions.
 *
 * Return: return 1 if n is greater than 0 and
 *   return 0 if the n is 0 otherwise return (-1)
*/

int print_sign(int n)
{
if (n > 0)
_putchar(43);
return (1);
if else(n == 0)
_putchar(48);
return (0);
else
_putchar();
return (-1);
}
