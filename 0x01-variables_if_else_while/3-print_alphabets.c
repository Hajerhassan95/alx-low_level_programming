#include <stdio.h>

/**
 * main - entry point
 *
 * Description: program prints alphabet with putchar function
 *
 * Return: Always 0 (Success)
*/

int main(void)
{
char ch = 'a';
char cH = 'A';


while (a <= 'z')
{
putchar(ch);
ch++;
}

while (A <= 'Z')
{
putchar(cH);
cH++;
}
putchar('\n');

return (0);
}

