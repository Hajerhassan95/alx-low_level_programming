#include <stdio.h>

/**
 * main - entry point
 *
 * description: the if statement program
 *
 * Return: 0 (Success)
*/

int main(void)
{
int ch = 97;
while (ch <= 122)
{
putchar(ch);
ch++;
}
putchar('\n');
return (0);
}
